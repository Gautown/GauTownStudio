---
import { render } from 'astro:content';

const markdownContent = `
# 测试标题

这是一个**加粗文本**的测试。

这是__另一种加粗文本__的测试。

这是*斜体文本*的测试。

这是_另一种斜体文本_的测试。

这是***加粗斜体文本***的测试。

这是**嵌套 [链接](https://example.com) 在加粗文本中**的测试。
`;

const renderedContent = await render({
  id: 'test',
  slug: 'test',
  body: markdownContent,
  collection: 'notes',
  data: {}
});
const { Content } = renderedContent;
---

<h1>Markdown渲染测试</h1>

<div>
  <Content />
</div>